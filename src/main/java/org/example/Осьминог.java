package org.example;

class Осьминог extends Mollusk {
    public Осьминог(String name, String uniqueness) {
        super(name, uniqueness);
    }

    // Метод uniqueness() будет вызван из Mollusk
}